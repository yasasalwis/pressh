// @pressh/adapter-sqlite — SQLite-as-DB StorageAdapter (placeholder).
// Distinct from the FS adapter's internal SQLite sidecar (which is part of core).
export const SQLITE_ADAPTER_VERSION = "0.1.0";
