/**
 * Pressh Studio admin client entry point. The client is assembled from modules
 * under ./admin/* (styles, markup, core routing, section renderers, designer).
 */
export { ADMIN_HTML } from "./admin/index.js";
