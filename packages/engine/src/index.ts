/**
 * @pressh/engine — content runtime.
 *
 * Phase 0 placeholder. Content types, entries, revisions, blocks, query
 * resolver, i18n, media, render pipeline, and GDPR service land in Phases 5–13.
 */
export const PRESSH_ENGINE_VERSION = "0.0.0";
