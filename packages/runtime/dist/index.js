// @pressh/runtime — plugin worker entry runner + iframe shim.
//
// `worker-entry` is the file Node spawns inside each plugin worker thread.
// `iframe-shim` is the small JS bundle served to plugin admin iframes,
// providing postMessage-based RPC back to the studio host.
export const RUNTIME_VERSION = "0.1.0";
//# sourceMappingURL=index.js.map