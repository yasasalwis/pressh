/**
 * @pressh/sdk — the public, semver'd surface plugins import.
 *
 * Phase 0 placeholder. Worker-side proxy, host-side registration, and the
 * built-in CSRF / capability / sanitization helpers land in Phase 8.
 */
export const PRESSH_SDK_VERSION = "0.0.0";
