// @pressh/sdk/host — host-side registration helpers.
//
// Used internally by core and engine to declare the RPC handler table
// that plugin workers can call into. Not for plugin authors.

export const SDK_HOST_VERSION = "0.1.0";
